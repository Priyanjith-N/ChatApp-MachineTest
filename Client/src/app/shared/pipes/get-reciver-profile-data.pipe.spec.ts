import { GetReciverProfileDataPipe } from './get-reciver-profile-data.pipe';

describe('GetReciverProfileDataPipe', () => {
  it('create an instance', () => {
    const pipe = new GetReciverProfileDataPipe();
    expect(pipe).toBeTruthy();
  });
});

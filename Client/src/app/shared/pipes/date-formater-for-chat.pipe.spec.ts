import { DateFormaterForChatPipe } from './date-formater-for-chat.pipe';

describe('DateFormaterForChatPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormaterForChatPipe();
    expect(pipe).toBeTruthy();
  });
});
